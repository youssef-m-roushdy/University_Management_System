// routes/instructorRoutes.tsx

import { AppRouteConfig } from './routesTypes';

// No instructor pages built yet — keep this file as the landing spot so
// protectedRoutes.tsx never needs to change when they're added.
export const instructorRoutes: AppRouteConfig[] = [];
